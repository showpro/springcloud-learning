server:
  port: 9002
spring:
  application:
    name: config-client
  cloud:
    config:
      label: dev #分支名称
      name: config #配置文件名称
      profile: dev #启用配置后缀名称
      uri: http://localhost:8905 #配置中心地址
#      安全认证用户名和密码
      username: zhan
      password: 123456