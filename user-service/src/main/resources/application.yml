server:
  port: 8201
spring:
  application:
    name: user-service
#  zipkin:
#    base-url: http://localhost:9411  # 配置收集日志的zipkin-server访问地址
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8001/eureka/